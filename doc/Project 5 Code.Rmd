---
title: "Project 5"
author: "John Podias"
date: "2022-12-08"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

EDA:

```{r }
rm(list=ls())
train <- read.csv("~/Documents/GitHub/Project-5/data/train.csv",stringsAsFactors=TRUE)
test <- read.csv("~/Documents/GitHub/Project-5/data/test.csv",stringsAsFactors=TRUE)

#check datatypes
str(train)
library(dplyr)
library(tidyverse)
library(psych)
head(train)
describe(train)
summary(train)
dim(train)
is.na(train)
#no vlaue svlaue
sum(is.na(train))
table(train$policy_id)

#check policy ID is unique
length(unique(train$policy_id))
#i need to only do this with the numeric values;
cor(train)


#max tor mac power

#engine type?
unique(train$engine_type)
```


```{r }

```

